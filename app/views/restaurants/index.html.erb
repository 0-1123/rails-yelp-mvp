<h1>Restaurants</h1>
<% @restaurants.each do |restaurant| %>
  <h2><%= link_to restaurant.name, restaurant_path(restaurant), class: "badge badge-dark" %></h2>
  <h4>Category</h4>
  <p><%= restaurant.category %></p>
<% end %>
